#include "main.h"
#include <stdio.h>
/**
 * add - Adds two integers and returns the result
 * @first_int: first integer to add
 * @second_int: second integer to add
 *
 * Return: Always 0
 */
int add(int first_int, int second_int)
{
	int sum;

	sum = first_int + second_int;
	return (sum);
}
