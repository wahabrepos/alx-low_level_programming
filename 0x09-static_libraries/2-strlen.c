#include "main.h"

/**
 * _strlen - Entry point
 *
 * Description: Returns the length of a string
 * @s: Pointer to a char
 * Return: Length of a string
 */

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
	}

	return (i);
}
